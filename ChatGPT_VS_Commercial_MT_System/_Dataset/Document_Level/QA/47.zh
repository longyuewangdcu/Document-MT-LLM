具体怎么做呢. 单片机外部中断和定时器冲突了？帮忙看看程序~外部中断有几率使定时器停止工作或不再中断 首先你的 Timer 1 是模式0（13位计数器），除非用很低频率的外部晶体，否则 Timer 1的定时时间根本就不是秒级。 其次你没有贴出time run标志被修改的位置。如果你怀疑Timer1没有持续工作，可以暂时去掉if( time run == 1 ) 条件， 观察数码管是否每次都显示变化的值。顺便说一句你的Timer0中断里面的语句效率低下，这不是一个健康的中断服务函数。 中断服务函数是要力求简洁明快的。 第二点，我曾经试过移除 time run ，但会出现的问题是相同的。 第三点，我试过将timer0的内容移到另一个函数去，然后在timer0里加上一个a=1， 主函数里加上if(a==1){这里运行刚才所说的函数}， 函数的第一行写了a=0，试图给中断服务程序减肥的，但程序动不了了。红外遥控的部分，因为时间关系摘的资料，所以也不是很会。
